# Keep the dependency graph honest: no silent hoisting of transitive deps.
# A package may only import what it declares (rule 34 — dependency hygiene).
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
